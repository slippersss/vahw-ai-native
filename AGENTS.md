# Agent entrypoint

Before doing work in this repository, read `skills/ai-native-meta/SKILL.md` completely and follow it.

For a concrete task, read only the relevant skills under `skills/`. Business repositories and task artifacts belong under the ignored `workspace/` directory. Keep reusable meta knowledge outside `workspace/`.

The primary business domain is vLLM Ascend, but business-specific knowledge must remain separate from workspace-wide meta rules.
